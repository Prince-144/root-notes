node_modules
.next
out
build
.DS_Store
*.pem
npm-debug.log*
.env*
!.env.example
.vercel
next-env.d.ts
tsconfig.tsbuildinfo

.vercel
.env*

# Local dev uploads. Production stores these in Vercel Blob, so anything
# that lands here is a local artefact, not content.
/media

# Generated Instagram assets — regenerate from the article, do not commit.
/instagram
